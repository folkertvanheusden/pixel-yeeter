pixelflood
----------

Pixelflood is mostly used in contests where the system that can push pixels the fastest, wins.
It is also usable as a generic protocol for drawing on e.g. LED panels.
The python library in this repository can push pixels via pixelflood as well.
